import React, {
    Component,
    PropTypes
} from 'react'

import {
    AppRegistry,
    StyleSheet,
    View
} from 'react-native'

import RootView from './app/root'

AppRegistry.registerComponent('SponBtv', () => RootView);
